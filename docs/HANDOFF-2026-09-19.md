# 交接文档（2026-09-20 会话收口 → 下一会话）

> **启动方式**：新会话输入 `continue`，Agent 读本文档即可接手。
> 基线：main @ 最新提交，工作树干净。
> 全量回归（169 测试 + 二十三套 e2e 全绿 + 基准 + TLA+ 门禁按预期色）。

---

## 0. 项目当前状态（Track A + Track B 均已闭合）

**basalt** = Kafka 协议兼容 Rust 流平台。

**Track B（多节点 HA）五项全部落地**——组协调器 HA（方案 B）闭合：
组状态权威存储 = 内部 topic（produce 路径 append=durable），协调器随分区
leader 迁移，failover 由重放恢复。§3.1 拓扑三面全部 ✅。

### 本轮（Track B）闭合面

| 面 | 状态 | 提交 |
|---|---|---|
| B2 组状态写内部 topic | ✅ __basalt_group_state 权威化（本地 OffsetLog 退役）+ 重放恢复 | `a112420` |
| B3 协调器选主 + failover | ✅ 监督循环重放重绑 + 3 节点 RF=3 kill -9 e2e（raft 引擎模式） | `9b8c8f2` |
| B1 内部口 HMAC 质询-应答 | ✅ 令牌不上线（v2 握手），明文 fail-closed | `8cc3804` |
| B4 share 状态持久化 | ✅ __share_group_state 事件流 + 重放（JSON 退役） | `417a489` |
| B5 块 e：ShareAck.tla | ✅ ack 状态机规约（名义绿 49,901 态 + phantom 突变体红）+ 终态优先修复（账本 67） | `ae449b1` |
| 账本 66/67 + HANDOFF | ✅ | 本提交 |

### 既有闭合面

Track A 五项（fsync=always 默认 / 时间 retention / 磁盘水位 / 连接指标 /
basalt-cli）——见 `40caa82` `ac2b12e` `f5c88eb` `5563273` `4bb4a26`；
协议兼容四客户端档 / 复制 failover / 事务 / classic+848+share 组 /
tiered+S3 实机 / fetch session / SASL+TLS / ACL / 配额 / 观测 L1（既有）。

### 本轮（第三会话）新增

| 面 | 状态 | 提交 |
|---|---|---|
| per-topic retention 透传 | ✅ CreateTopics configs（retention.ms/bytes）→ WAL → LogOptions；DescribeConfigs 回真值（替代硬编码假值） | `2f2f126` |
| KIP-932 多节点 share 协调 | ✅ **落地**（服务端批切片 + snappy 解压 + 状态新鲜度门；run_share_failover.sh PASS） | `9c06fe5` |
| basalt-cli groups 状态列 | ✅ ListGroups v4 + 空过滤器语义 + 字段名对齐 | `8feb6e8` |

### 账本新增（Track B 轮）

| # | 缺陷 | 状态 |
|---|---|---|
| 66 | CreateTopics ReplicationFactor（线上 int16）用 as_i32 窄匹配静默返 0 → rf=0 无副本分区 → failover 无候选人（㊿ 家族第二例） | ✅ 修复 + scripts/audit_field_types.py 系统性审计门禁 |
| 67 | share ack 终态遮蔽：accept 超集越过部分 reject 的归档（Reject 永久性破坏）/ 对称面 accept 后被归档——ShareAck.tla 首跑抓到 | ✅ 修复（终态优先：accept 剔归档、reject 剔已 accepted） |

---

## 1. 已确认设计决策

| 决策 | 结论 | 依据 |
|---|---|---|
| 组协调器 HA | **方案 B**：内部 topic + 重放恢复 | TLA+ GroupStateHA 确认模型 |
| 内部 topic 持久化 | **复用 produce 路径**（append=durable 原子） | TLA+ v1 violation = 建模 artifact |
| fsync 默认档 | **always（SyncEach）**，os 显式退回 | Track A1；基准 -17% = 持久化诚实代价 |
| TLA+ 纪律 | 规约先行 + 突变体判别力 + 审计脚本门禁 | GroupStateHA / ShareAck / audit_field_types |
| 组协调器随分区 leader 迁移 | B3 落地（监督循环：leader==self 且 epoch 变化 → 重放重绑） | 方案 B + raft 引擎控制器选主 |

---

## 2. 下一步

Track A/B 主干 + KIP-932 多节点 share 协调均已闭合。剩余实质项：

| 候选 | 说明 | 规模 |
|---|---|---|
| 增量元数据同步（T-M2.2 残项） | broker 200ms 全量快照拉取 → delta 增量（按 version 追增量 record 流） | 1 |

已评估关闭（技术结论，无需开发）：
- 组再均衡事件持久化——成员关系重启即失 = 客户端重 join 语义等价
  （与 Kafka 消费组行为一致），持久化无增量价值；
- KIP-848 assignment 持久化——同上（848 心跳面自带再入组语义）；
- time retention 长尾 active 段滞留——Kafka 同型行为，非缺陷。

### Track A/B 已知遗留优化项（非阻塞）

- time retention 长尾低流量 topic 的旧数据滞留 active 段（Kafka 同型，非缺陷）
- basalt-cli groups 的 STATE 列（broker 未回填 state 字符串，显示为空）
- share 交付切片 v1：压缩批解压后切片重编码为未压缩（lz4/zstd/gzip/
  snappy 均支持；snappy 为 KIP-375 xerial 框架——franz-go 默认档实测）
- 静态控制器模式（默认）下控制器节点单点——生产多节点部署应启用
  `BASALT_CTRL_RAFT_ENGINE=raftrs`（B3 e2e 同款；raft 引擎有真选举）

---

## 3. 关键架构知识

### 3.1 状态面拓扑（Track B 后）

```
ClusterState（topics/assignments/leaders）
  → Controller WAL/raft → 所有 broker 200ms 快照拉取
  → ✅ HA（raft 复制 + failover；raft 引擎模式有真选举）

Partition 数据（segments）
  → leader 写 → follower 拉 → ISR 多数确认
  → ✅ HA（多副本 + failover 选举）

组协调状态（offsets / share cursor）
  → __basalt_group_state / __share_group_state（内部 topic，produce 路径）
  → ✅ HA（方案 B：协调器 = 分区 leader；切换时重放恢复）
    · GroupManager commit/promote → state_out 通道 → GroupStateSync 转发
      （acks=all，应答 Ok = 已持久化；失败回 CoordinatorNotAvailable）
    · 监督循环（main.rs）：leader==self 且 epoch 变化 → 重放 + BindState
    · share ack 事件 → SHARE_SINK 通道 → 异步下沉（事件丢失 = 重投递，
      KIP-932 至多一次容忍）
```

### 3.2 通道拓扑

```
每分区独立 actor（mpsc 1024）→ 数据面分区间隔离 ✓
GroupManager（mpsc 512）+ state_out 下沉通道 → 组面
CG848 actor（mpsc 256）→ 848 组面（内存态，重启即失 = 重 join 语义）
MetaService（mpsc 256）→ 元数据面
Controller（mpsc 256）→ 控制面（静态模式）/ raft 引擎（raftrs 模式）
TxnCoordinator（mpsc 256）→ 事务面
```

### 3.3 端口约定（陷阱 26）

```
主口 = BASALT_PORT（默认 9092）
内部口 = 主口 + 1（9093）——metrics/测试端口必须避开
metrics = BASALT_METRICS_PORT（默认 9094；0 = 关）
内部口鉴权 = BASALT_INTERNAL_TOKEN（设置后走 v2 HMAC 质询-应答，
  明文握手 fail-closed 拒绝）
```

### 3.4 TLA+ 规约索引

| 规约 | 建模 | 状态 |
|---|---|---|
| BasaltDataPlane.tla | 数据面 produce/fetch/replicate | ✅ 绿 |
| ConsumerGroup.tla | classic 四态机 + fencing | ✅ 绿 |
| ConsumerGroup848.tla | KIP-848 + 生命周期硬化 | ✅ 绿 + 两突变体红 |
| TransactionCommit.tla | 两阶段事务 + marker + LSO | ✅ 绿 + 两突变体红 |
| ReplicationCommit.tla | HW/ISR/reconciliation | ✅ 绿 |
| GroupStateHA.tla | 组状态内部 topic + 重放 | ✅ 确认模型绿 |
| ShareAck.tla | share ack 状态机（块 e） | ✅ 绿（49,901 态）+ phantom 红判别 |

---

## 4. 构建陷阱（累计）

### 09-19 第一轮（17-22，Track A 前轮）

17-22 条（produce 处理序 / CreateTopics 默认覆盖 / 兜底错误码 / SCRAM
nonce / 重启首请求 / PARTITION_GAUGES 类型）见 git 历史版本本文档。

### 09-19 第二轮（23-27，Track A）

23. **账本 64**：稀疏索引末项 ≠ 段内最大时间戳——时间判定要有不依赖
    索引的独立信号（mtime）作合取；单信号判删即误删。
24. **账本 65**：编码器按计划字段名查值、查不到静默写默认值——入口必须
    校验；字段名以官方协议 JSON 为唯一权威。
25. e2e 清理遗留进程用 `pkill -x`（精确进程名）；`pkill -f` 会匹配脚本
    自身快照命令行 → 自杀，复合命令静默中断（后续命令没跑 =「改了
    没生效」假象）。
26. 端口撞车：内部口 = BASALT_PORT+1；新增监听面先查「端口 +1/+2 占用
    约定」。
27. 改默认值的连锁面：fsync 翻转影响基准基线与 e2e 语义覆盖——改默认
    必须同步 BENCHMARKS.md 与文档注释。

### 09-20 第四轮（28-31 不变，新增 32）

### 09-19/20 第三轮（28-31，Track B）

28. **账本 66**：协议字段类型审计必须系统性（脚本比对官方 JSON），
    人工逐字段 review 追不上「同族缺陷逐一复发」的节奏（㊿→66 两例）。
29. **账本 67**：终态 ack 无优先级 = 状态机被重复 ack 污染（accept/reject
    双向遮蔽）。共享可变状态机的每个动作都要定义「与既成终态相遇时谁赢」。
30. 多节点 e2e 的场景前提要写进脚本头注释：run_group_failover 依赖
    raft 引擎模式（RAFTRS=raftrs）——批跑忘传参数 = 场景静默退化
    （failover 语义在静态控制器模式下根本不存在）。
31. tokio mpsc 通道元素类型在 async block 内解构时偶发推断失败——
    显式标注 `(Sender<T>, Receiver<T>)` 或先 bind 后解构。
32. **多节点 share 分裂面**（第三会话定位）：组级状态（成员/会话/交付
    游标）驻协调器节点、数据面（ShareFetch/Ack）由客户端发往分区
    leader——单节点同址掩盖一切；跨节点拆分必须先问「每个请求落在
    哪个节点、它有没有该请求所需的状态」。验收资产先行：
    run_share_failover.sh（现为 SKIP/77）+ franz-go failover 模式。

---

## 5. 快速验证命令

```bash
cd /home/nzinfo/src.db/stream-db/basalt
cargo test --workspace                      # 169 测试全绿
python3 scripts/audit_field_types.py        # 协议字段类型审计（账本 66 门禁）

# per-topic retention（第三会话）
bash testing/e2e/run_retention_pertopic.sh  # A 题过期 / B 题默认不误伤 / configs 回显
RAFTRS=raftrs bash testing/e2e/run_share_failover.sh   # B5 验收测试（现 SKIP/77——缺口见 §2）

# 核心面
bash testing/e2e/run_e2e.sh                 # 基础全链
bash testing/e2e/run_durability.sh          # A1 fsync=always kill -9 不丢
bash testing/e2e/run_retention.sh           # A3 时间 retention
bash testing/e2e/run_watermark.sh           # A2 磁盘水位
bash testing/e2e/run_conn_metrics.sh        # A4 连接指标
bash testing/e2e/run_cli.sh                 # A5 basalt-cli
bash testing/e2e/run_group_state.sh         # B2 组状态内部 topic 权威化
RAFTRS=raftrs bash testing/e2e/run_group_failover.sh   # B3 协调器 failover
bash testing/e2e/run_internal_auth.sh       # B1 内部口 HMAC
bash testing/e2e/run_share_state.sh         # B4 share 状态内部 topic
bash testing/e2e/run_franzgo.sh             # franz-go 全组协议 + 事务
bash testing/e2e/run_kafkaclients.sh        # Java 官方栈
bash testing/e2e/run_auth.sh run_acl.sh run_quota.sh run_tls.sh
bash testing/e2e/run_share_spike.sh         # KIP-932 share 全链

# 存储面（run_tiered_s3 前置：durd --s3-addr 127.0.0.1:19000 信任模式）
bash testing/e2e/run_tiered.sh run_tiered_pertopic.sh run_tiered_retention.sh
bash testing/e2e/run_tiered_s3.sh

# 组协议 / 多节点
bash testing/e2e/run_franzgo_848.sh run_franzgo_coop.sh
bash testing/e2e/run_multinode.sh           # 多节点 failover（默认静态控制器）
RAFTRS=raftrs bash testing/e2e/run_multinode.sh  # raft 引擎模式

# 基准 + TLA+
bash testing/bench/run_bench.sh             # always 档：produce ~1400 MB/s
make -C spec group-state-ha                 # 绿
make -C spec consumer-group-848 consumer-group-848-nohbfence consumer-group-848-nocommitfence
make -C spec share-ack                      # 绿（49,901 态）
make -C spec share-ack-phantom              # 红（判别力）
```

---

## 6. 关键文档索引

| 文档 | 内容 |
|---|---|
| docs/adr-22-security-admin-plane.md | 安全面/管理面/配额一期 |
| docs/adr-21-tiered-storage.md | 分层存储 v1/v1.1/v2 |
| docs/adr-19-kip848-consumer-groups.md | KIP-848 四块 |
| docs/adr-18-transactions.md | 事务 ADR |
| docs/assessment/production-readiness-20260919.md | 生产就绪评估 + P0-P2 缺口分级 |
| docs/VERIFICATION.md | 账本 ⑩-㊿ + 51-67 |
| docs/BENCHMARKS.md | 吞吐基线（含 fsync=always 档） |
| docs/research/2026-09-18-share-groups-evaluation.md | KIP-932 评估纪要（块 b-e 定义） |
| README.md | 含 basalt-cli 用法 |
| cli/src/main.rs | 运维 CLI（字段名权威 = protocol/definition/*.json） |
| server/src/group_sync.rs | 组状态/share 事件下沉 + 重放（方案 B b2） |
| spec/ShareAck.tla | share ack 状态机规约（块 e） |
| spec/GroupStateHA.tla | 组状态 HA 规约（方案 B 确认模型） |
| scripts/audit_field_types.py | 协议字段类型审计门禁（账本 66） |
| testing/e2e/ | 二十三套 e2e |
| testing/bench/run_bench.sh | 吞吐基准 |
