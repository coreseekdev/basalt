# 交接文档（2026-09-18 会话收口 → 下一会话）

> 用途：新会话/新 Agent 的第一个输入。读完本文即可接手工作。
> 基线：main @ 本文件提交的前一提交，工作树干净，已推送 origin。
> 上一份：`HANDOFF-2026-09-17.md`（§1 构建陷阱有效）。本文覆盖 09-18
> 全天五轮：T-M3.3 闭合（b/c/d）→ T-M3.4 闭合 → T-M4.3 v1 → T-M3.5 →
> **T-M4.1/T-M4.2 一期（安全/管理面/配额/TLS，ADR-22）**。

## 0. 最新轮（安全面/管理面/配额一期，ADR-22）✅

- **SASL/SCRAM-SHA-256 接线**：SaslHandshake(17)/SaslAuthenticate(36)
  handler + 未认证门禁（仅 18/17/36 放行，越权断连）+ 断连语义
  （58/33/34 响应先行、watch 通道通知读侧）。server-final `v=<sig>` 补齐
  （kafka-python/franz-go 均校验）。已知差异：kafka-python 2.2.3 SCRAM 走
  pre-KIP-152 裸交换（SaslHandshake v0 + 裸 token）——不兼容，留 P2。
- **TLS listener**（rustls/tokio-rustls，独立口 client+2，主口保持明文）：
  `BASALT_TLS_CERT/KEY`；**通告跟随所连 listener**（broker_array 本节点
  强制连接级 host/port，账本 57——否则 SSL 客户端被重定向明文口）。
  serve_connection 泛型化。
- **配额**：逐连接字节率 token bucket（`BASALT_QUOTA_{PRODUCER,FETCH}_BYTES`）。
  两结构性要点（账本 55）：**持锁休眠**（tokio Mutex，pipeline 请求在锁上
  排队按速率串行化）+ **负债制**（先扣减可为负；休眠期回补不再产生可消费
  额度）。e2e：3MB @1MB/s 实测 2.40s（精确速率）。
- **管理面**：DescribeCluster(60 v0-2) + DescribeConfigs(32 v1-4，静态投影
  DEFAULT_CONFIG/ReadOnly 如实标注) + ClusterId 持久化（data_dir/cluster_id，
  Metadata 同步回填非 Null）。
- **e2e 三档 + 基准**：`run_auth.sh`（librdkafka SCRAM 正/负路径 + 门禁 +
  存活）、`run_quota.sh`、`run_tls.sh`（openssl 现生成证书 + librdkafka SSL
  全链 + 明文口共存 + 明文打 TLS 口被拒）；`testing/bench/run_bench.sh`
  （release 基线：produce 1703MB/s、consume 1GB/s）。
- **账本 58（基准副产，当轮修复）**：produce 处理序乱序 → OOOSN 自激级联
  （吞吐假象 + 重复双重表现同根）；PRODUCE 读循环内联（处理序=读序），
  修复后 1703MB/s / 零重复 / 全量回归绿。
- **账本 55/56/57**：配额 pipeline 稀释+休眠回补双计；TLS select 空臂
  瞬时就绪→启动即停机（pending 兜底）；listener 通告重定向。

## 0.5 前序轮完成（同日早前四轮）

## 0. 本轮完成（v2 消费组双协议 + 分层存储 + 生态负载面）

**T-M3.3 KIP-848 ✅ 闭合**（`5a6199e`/`47788d0`/`13344d7`）：块 b 协议面
（68/69 handler + 宣告 + 字节级回归）；块 c 验收（franz-go v1.21.6 三验收
面；客户端门槛定案 franz-go 先行、宣告 0-1；**codec nullable-struct 原语
缺陷，账本 51**）；块 d 规格（ConsumerGroup848.tla 三门禁，两突变体正交红）。

**T-M3.4 cooperative-sticky ✅ 闭合**（`ac99452`/`d6d6dd6`/`968a379`）：
848 侧 ServerAssignor 协商（112 经 kerr 双源核对）+ uniform movement-
minimizing；classic 侧协议选择 = leader 偏好 ∩ 全体支持集（去硬编码
range）+ 协调器 pending join/sync 悬挂修复（账本 52）；e2e coop 档。

**T-M4.3 分层存储 v1 ✅**（`5ae39cc`，ADR-21）：前置阅读硬性项完成
（Arroyo §11 CAS+权威记录 / Morax §6 不可变对象+孤儿无害，两路并评——
取前者，RDS 权威按 Morax 警示排除）。ObjectStore 四原语（create CAS
冲突回读）+ LocalFs/Memory 双实现 + 分层注册表（写序=段对象→段记录→
本地回收；启动期孤儿 GC）+ Log::release_sealed（回收不动 log_start）+
读穿透/FetchSlice 分流/重启恢复 + tiered e2e（400 条全链 PASS）。
**联调副产两笔账本**：53（幂等 last_seq 记成 base——多记录批次第二批判
45）/54（终态数据 fence 过强 vs librdkafka 一 epoch 多事务——语义修订为
epoch 单调，ADR-18 §14 注记，两 fence 放宽 + 测试重写）。

**T-M3.5 生态真实负载 e2e ✅**（`0933a71`）：三模板全过（rdkafka 手动
assign + 事务隔离 / Vector 式 drain-then-commit + 崩溃批重放 at-least-
once / Bento 式 checkpoint_limit 背压）+ librdkafka cooperative 档
（T-M3.4 遗留闭合）+ **docker-compose 一键 4/4 PASS**（Dockerfile.e2e
宿主二进制装载式；离线 override 用宿主 glibc loader 直启——本环境 docker
hub 不可达、宿主 glibc 2.43 高于全部本地镜像）。

**P1 尾项**：事务仿真默认 seeds 50→25（CI 档 80s→42s，TLA+ 三门禁兜底）。

**追加两轮（同日）**：
- **参考实现对照研究**（`92e3e15`，docs/research/2026-09-18-reference-
  comparison.md）：kafka trunk（KIP-848 GA + KIP-405）/ redpanda v26.3 /
  slatedb / duramen 四路深潜，行动清单 P0-P2 全量落地。
- **T-M3.3.1 848 生命周期硬化 ✅**（b65c809）：session timeout（P0——
  崩溃分区泄漏封口）、撤销确认闭环（P0——双 owner 消除）、差分下发
  （Assignment=null）、prev-epoch 容错/epoch-0 rejoin、Describe 三值、
  interval 节流、组容量 81、SubscribedTopicRegex 同步解析、静态成员
  （-2 + InstanceId 顶替 rejoin）、rebalance-timeout fence（宽限到期注销
  僵尸 owner，非仅放行分区）；coordinator 测试 8→16；已知语义边界：
  fence 应答 MemberEpoch=请求原值（Kafka GMM:4691）与 basalt 回带服务端
  epoch 的差异见 research §2.4。
- **T-M4.3.1 tiered v1.1 ✅**（同提交）：段 key 编入 leader epoch（漂移
  不再覆盖）、上传异步化（OffloadJob + OS 线程 offloader + 字节配额——
  tokio::spawn 内嵌 block_on 会 panic，弃用）、运行期孤儿 GC（mtime
  宽限窗）、读穿透 range 化（记录带 crc32c + 稀疏索引）。
- **duramen S3 适配器 ✅**（P2）：arrow-rs 0.14 薄层（feature
  objstore-s3 默认开）；实机联调发现并修复 duramen **Last-Modified 头
  ISO-8601→RFC 2822**（duramen 2f2a8ec——arrow-rs/AWS SDK 严格按
  HTTP-date 解析，head()/get_range 全挂）；实机全链：produce→上传→
  读穿透→重启恢复 400/400 PASS。

**P1 尾项（更新）**：fresh/ISR 双阈值分离建模维持「按需」降级（未做）；
P1 848 差分下发等原清单项已全部完成。

**验证状态**：`cargo test --workspace` 18 套件全绿（server bin 53、
storage +12、coordinator consumer_group ×8、txn 27）；六套 e2e 全 PASS
（kafkaclients/franzgo/848/coop/tiered/docker 4 模板）。

**share groups spike ✅（2026-09-19，GO）**：块 a+b 核心（76/78/79 +
share_group.rs 内存交付面）+ run_share_spike.sh 四验收面全 PASS。两条
协议实证：① ShareFetch 沿用 KIP-227 增量会话——服务端必须维护会话注册
集（session_register）且以之为服务面；② ShareSessionEpoch ≠ member
epoch——校验仅做成员存在性。残留（评估纪要 §4.5/块 c-d）：状态持久化、
多成员 rebalance 迁移、遗忘分区注册删除、长轮询。

## 1.5 生产就绪评估（2026-09-19，docs/assessment/production-readiness-20260919.md）

四项评审结论：P0 缺口五项（内部口无鉴权 #1 / 断电窗口 #2 / 组协调 HA #3 /
观测缺口 #4 部分速赢 / 磁盘满 #5）；解耦主路径安全（数据面拖不死管理面），
风险 = 连接内混用（账本 58 已知情）+ 管理面三单 actor（多节点规模化显性）；
可观测 L1-L5 分层框架已设计（L1 指标基建 + L2 管理面观测为下一对会话候选）。
速赢已落地：消费指标假值修复、METRICS_PORT=0 语义、/health /ready、26 处
eprintln 迁移 tracing。**share groups spike 的四条设计约束见评估 §6。**

## 1. 下一会话的工作（按优先级）

### P0 ✅ 已修复（当轮闭合，账本 58）：produce 处理序乱序 → OOOSN 级联
「幂等去重跨重连失效 / PID 持久化」初判方向**作废**（非事务 PID
per-session 是 Kafka 本尊语义）。真根因：conn.rs per-request 并发派发下同
连接 pipeline 的 produce 在分区 actor 邮箱竞速、后发先至 → 服务端正确回
OOOSN → kgo 整 PID reload + 回卷重发（=重复）+ 元数据 5s 停等 +
produceTimeout 丢批（=吞吐假象 13.3MB/s）。修复：PRODUCE 读循环内联
（处理序=读序）。修复后 bench produce 1703MB/s（93×）、200000/200000 零
重复、全套件 + 九套 e2e 回归全绿。教训：吞吐「优化」诉求先查处理序缺陷。

### P1 ✅ 当轮闭合：T-M4.1 验收矩阵（run_sasl_matrix.sh 三客户端六项全 PASS）
连带修复三笔：**59** CreateTopics 请求 NumPartitions/RF 被 broker 默认覆盖
（EnsureTopic 新命令）；**60** 空日志 read_ex NotFound→15→消费 strip 死循环
（空读守卫；「兜底错误码就是客户端行为的编程」）；**61** SCRAM server
nonce 字符集非法（Java 严格解析暴露；nonce 重写+单测）。kafka-clients
3.7.0→3.9.1（JDK 25 兼容，KAFKA-16443）。全套件 + 十套 e2e 回归全绿。

### P0 ✅ share groups 评估完成（纪要：docs/research/2026-09-18-share-groups-evaluation.md）
触发条件达成：三客户端支持度实测全就绪（librdkafka 2.15 / kafka-clients
3.9.1 早鸟 / franz-go v1.21.6 ShareGroup+Record.Ack）。结论：**建议立项**
（≈4-5 会话；块 a 76 handler 复用 848 actor → b 分区侧区间化 ack 状态机 →
c share-state log 持久化 → d 三客户端 e2e → e 可选 TLA+；spike 先行 =
块 a 最小面 + franz-go 拉通 heartbeat/fetch/ack）。**已批准立项
（2026-09-18），排序在 T-M4 尾项（fetch session / ACL 骨架 / S3 实机硬化）
之后**——执行顺序即 tail → spike → 全量。

### P1
- ~~produce acks=all 吞吐~~ ✅ 账本 58 修复（1703MB/s，见 §0 P0 段）；
- per-user 配额 ✅（T-M4.2 尾项：BASALT_QUOTA_USER_BYTES + run_quota.sh 阶段二）；
- **fetch session ✅（KIP-227，T-M4.2 尾项）**：fetch_session.rs（epoch 0 建/≥1 增量省略空分区/-1 关；70/71 + session_id=0 优雅降级）；v1 边界——请求外注册分区仅被动记账、HW 变更条目不回填（模块注释明示）；Java/librdkafka 真会话面十二套 e2e 回归绿；
- **ACL 骨架 ✅（T-M4.1 尾项）**：acl.rs + 三 Admin API + 数据面闸门五处（produce/fetch/create/delete/join；连带修复账本 63——fetch v13+ id-only 授权空名反查）；e2e run_acl.sh 六验收面（SCRAM + authorizer 全开正/负路径）；
- T-M4.1 尾：franz-go/kafka-clients 的 SASL+TLS 矩阵档；ACL 骨架；
- T-M4.2 尾：per-user 配额、fetch session；
- T-M4.3 v2 边界（ADR-21 §3）：**per-topic storage-mode ✅ + 对象侧
  retention ✅ 当轮双闭合**（run_tiered_pertopic.sh / run_tiered_retention.sh；
  连带修复账本 62 单节点重启 MetaService 空簇——启动期 refresh 补齐）；
  S3 实机硬化 ✅（durad 双模式：run_tiered_s3.sh 信任模式全链 + SigV4 签名
  读写；前置 = durad 19000 信任模式 + 桶 basalt-tiered，启动命令见
  duramen/docs/RUNBOOK.md）；T-M4.3 v2 面全清；
- fresh/ISR 双阈值分离建模（有判别场景时再做）。

### P2 打磨
kafka-python 裸 SCRAM 兼容（pre-KIP-152 裸交换：SaslHandshake v0 后接
长度前缀裸 token——需读侧帧多路分解）；SCRAM-512；分区 actor 孤儿数据
目录 retention 清理验证；重试风暴夜间扩量；848 面 Leave 身份校验。

## 2. 陷阱（本轮增量，接 09-17 handoff §2；51-54 详见账本 §12）

1. **MetaCmd::Lookup 的 None/Some 二义**：`names: None` = 全量 topic。
2. **KIP-848 keepalive**：未变字段置 null——订阅 null ≠ 退订。
3. **nullable struct 线上原语**（51）：int8 presence 标记，与 flexible
   无关；布局探针不够，须真客户端过一遍（自 round-trip ≠ 互操作）。
4. **franz-go 848 三道门**：ctx opt-in + eager balancer 首位 + 宣告 68
   max≥1；错误码以 kerr/Errors.java 为权威（112 非 57）。
5. **TLA+ 审计位等式 RHS 加括号**（`x' = a \/ b` 解析陷阱）；突变体
   捕获面要落准；TLC `#` 不做跨类型比较。
6. **幂等 last_seq = 批内末序号**（53）：base 语义下多记录批第二批判 45；
   单批会话的 e2e 永远测不出。
7. **终态 fence = epoch 单调**（54）：同 epoch 终态后新事务批/marker
   合法（librdkafka 多事务）；marker 与 produce 跨路径无顺序面——
   marker 先落时后到数据以新 first_offset 重锚 txn_open（LSO 照常锚定，
   60s 自 abort 兜底）。
8. **HeartbeatResult 是快照**：多步组断言读 actor 现值；探针先发齐全部
   请求再 await（串行 await 会撞 rebalance deadline 被 27 冲刷）。
9. **e2e 环境坑**：topic 名勿撞 find 过滤子串；segment_max_bytes 滚动
   按批粒度——单批超限不滚动，produce 须分批 flush；改 server 代码后
   忘 rebuild = 旧二进制假象（本环境 port 占用即此因）。
10. **本环境客户端面**：confluent_kafka 2.15 wheel 禁用 rebalance_cb
    （协作语义改行为断言）、ru 下也过滤已中止数据（ru 对照交 franz-go 档）；
    docker hub 不可达 + 宿主 glibc 2.43 → 容器用宿主二进制 + loader 直启
    override。
11. **节流 = 串行化 + 负债**（55）：sleep 型限流必须持连接锁休眠（pipeline
    并发各睡各的 = 吞吐×并发）且 token 允许负（休眠期回补被睡醒请求瞬时
    吃掉 = 速率翻倍）；两 bug 叠加时 e2e 断言只红一次，修一个还红。
12. **select! 新臂必须问「未命中时它挂不挂起」**（56）：None→async{} 瞬间
    就绪，随机选中即优雅停机——进程启动 2s 自杀且无 SIGTERM 日志。
13. **通告地址跟随所连 listener**（57）：metadata broker 地址若取自集群
    缓存，TLS 口客户端会被重定向明文口；broker_array 对本节点强制用
    连接级 host/port。
14. **kafka-python SCRAM 是裸交换**（pre-KIP-152）：门禁把 `n,,` 当 api_key
    断连不是门禁 bug——kafka-python 不走 SaslAuthenticate；SCRAM e2e 用
    librdkafka/franz-go。
15. **幂等 seq 的前提 = produce 按连接序应用**（58）：响应序重排（㊷）不
    等于处理序有序——并发派发下 pipeline produce 在 actor 邮箱竞速，
    「服务端正确回错」本身成为客户端 reload 风暴的扳机。PRODUCE 已改读
    循环内联；新增 API 时问一遍：它的语义是否依赖连接内处理序？
16. **吞吐类缺陷双重表现互为掩护**（58）：重复（正确性）与慢（性能）同根
    ——先修正确性再看性能数字；「客户端日志 + 服务端插桩对拍」是锁定
    乱序/丢批的判别面（produced 成功 vs 服务端缺口的矛盾即铁证）。

## 3. 快速验证命令

```bash
cd /home/nzinfo/src.db/stream-db/basalt
cargo test --workspace                      # 全套件绿（server bin 60）
bash testing/e2e/run_kafkaclients.sh        # PASS（[0]-[8]）
bash testing/e2e/run_franzgo.sh             # PASS（[1]-[7]）
bash testing/e2e/run_franzgo_848.sh         # PASS（KIP-848 三验收面）
bash testing/e2e/run_franzgo_coop.sh        # PASS（cooperative-sticky）
bash testing/e2e/run_tiered.sh              # PASS（分层存储全链）
bash testing/e2e/run_e2e_docker.sh          # PASS（compose 一键 4/4）
bash testing/e2e/run_auth.sh                # PASS（SCRAM 门禁正/负路径）
bash testing/e2e/run_quota.sh               # PASS（1MB/s 限流精确生效）
bash testing/e2e/run_tls.sh                 # PASS（TLS 全链 + 明文共存）
bash testing/e2e/run_sasl_matrix.sh         # PASS（SASL/TLS 三客户端矩阵六项）
bash testing/e2e/run_tiered_pertopic.sh     # PASS（per-topic 分层双题分叉）
bash testing/e2e/run_tiered_retention.sh    # PASS（对象侧 retention 四验收面）
bash testing/e2e/run_acl.sh                 # PASS（ACL 授权门禁六验收面）
bash testing/e2e/run_tiered_s3.sh           # PASS（S3 实机，前置 durad@19000 + 桶 basalt-tiered）
bash testing/e2e/run_share_spike.sh         # PASS（KIP-932 share 全链 spike）
bash testing/bench/run_bench.sh             # 吞吐基线（release）
make -C spec consumer-group-848 consumer-group-848-nohbfence \
      consumer-group-848-nocommitfence      # 一绿两红
```

## 4. 关键文档索引

- ADR-19/20/21（T-M3.3/T-M3.4/T-M4.3）：`docs/adr-1{9,20,21}-*.md`
- ADR-18 §14（fence 语义修订）：`docs/adr-18-transactions.md`
- 账本 51-54：`docs/VERIFICATION.md` §12
- 分层存储：`storage/src/{object_store,tiered}.rs`；e2e：`testing/e2e/`
- T-M3.5 模板语义对照：`testing/e2e/README_T35.md`
