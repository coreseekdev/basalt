# 交接文档（2026-09-16 会话收口 → 下一会话）

> 用途：新会话/新 Agent 的第一个输入。读完本文即可接手工作。
> 基线：main @ `1373ac3`，工作树干净，已推送 origin。

## 0. 必须遵守的工作流（用户点名）

**Review 流程**（本仓库的核心纪律，每个工作块收口前必须执行）：

1. **周期性 code review agent**：派 general-purpose 子代理审阅最近的提交/指定模块（本会话覆盖了 partition.rs/meta.rs/conn.rs/raftrs_engine.rs 等）。历史产出形态见 `docs/review-*.md`（四轮范例：`review-adr14-pool-20260910.md` 等）。
2. **每个发现必须探针实证**：临时探针跑出红/绿证据（含对照组防恒真——"检查器自身要活着"），探针用后即删、工作区干净。
3. **修复 + 回归锁定**：每个缺陷配确定性单测或 e2e 场景（永久检测机制）。
4. **账本登记**：`docs/VERIFICATION.md` §12 缺陷→检测机制矩阵（格式照 ㉒-㊻ 条目）；A开会话新发现的缺陷接着编号。
5. **commit & push**：中文 commit message，一个逻辑单元一个提交，直接推 main。

**验证纪律**：
- 规格变更 → 重跑五门禁：`make -C spec replication-commit replication-commit-norecon replication-commit-liveness replication-commit-recovery`（前两道必须一绿一红——阴性对照红才算判别力活着）。
- 实现变更 → workspace `cargo test --workspace` + 相关 e2e。
- ⚠️ 历史教训：**「全空间绿」可能是平凡绿**（崩溃路径不可达时安全不变式无判别力）——不变式必须配探针/阴性对照验证检查器自身（账本 ㉟ 规格化条目的 UNCHANGED 污染教训）。

## 1. 仓库与构建

- 路径：`/home/nzinfo/src.db/stream-db/basalt`；workspace 8 crates（record/protocol/storage/metadata/coordinator/server/verification/benches）。
- 工具链：Rust stable；python3.14（kafka-python、confluent-kafka 已装）；Go 1.27（franz-go，模块已拉）；Java 17 + Maven（kafka-clients 3.7，本地 ~/.m2 已缓存）；TLC：`spec/tools/tla2tools.jar`（verify.sh 用 `TLATOOLS` 环境变量）。
- e2e 入口：
  - 单节点：`bash testing/e2e/run_e2e.sh <script.py>`
  - 多节点（默认模式）：`bash testing/e2e/run_multinode.sh <script.py>`
  - 多节点引擎模式：`RAFTRS=1 bash testing/e2e/run_multinode.sh <script.py>`（runner 注入 BASALT_ISR_LAG_MS=500）
  - 客户端档：`run_franzgo.sh`、`run_kafkaclients.sh`
- Spec 门禁：`make -C spec replication-commit replication-commit-norecon replication-commit-liveness replication-commit-recovery`（后缀 -norecon 是阴性对照，必须红）。

## 2. 当前状态（截至 1373ac3）

**v1（M0-M2 + T-M3.1）已完整闭合**，本轮新增：
- **控制器 raft WAL**（ADR-17，dendro SPEC 02 移植子集）：`server/src/ctrl_raft/wal_log.rs`——WalStorage 写穿（帧头 32B/crc-payload-only/段轮转/末段撕裂容忍），快照降级为状态机种子。
- **ISR 状态机 + 冻结提交面 + 就任拉齐**（账本 ㉟ 完整修复）：partition.rs 显式 ISR 集合、PendingReplica 长轮询、幂等去重。
- **FetchSlice 长轮询**（T-M2.4 首项）：3 副本 acks=all 249K msg/s、延迟 p50=1.0ms（基线的 1/150）。
- **四档客户端**：kafka-python / librdkafka / franz-go / kafka-clients（Java，`testing/kafkaclients/`）——各档均有战果（⑩⑪⑫㊷㊸㊹㊺）。
- **DeleteTopics 完整语义**（㊺）：ClusterRecord::DeleteTopic 全链 + 孤儿 actor 关停。
- **幂等 producer 服务端去重**（T-M3.1）：partition.rs 幂等状态机（PID → epoch/last_seq/最近 5 批），PID 跨 broker 唯一（node_id 高位组合）。
- **ReplicationCommit.tla 五门禁**：安全名义绿 + 判别力阴性对照红 + 名义活性绿 + 恢复活性绿（UNCHANGE 污染修复后 39,802 态全空间）。
- **CI**：verification.yml——spec-tlc 五步 + e2e-clients 双模式（默认 5 场景 + 引擎 9 场景）+ opfuzz-nightly。

**账本**：㉒-㊻ 共 26 个新缺陷全部闭环（docs/VERIFICATION.md §12），含 3 个规格化相关（UNCHANGE 污染/判别力/活性反例）。

## 3. 下一会话的工作（按优先级）

### P0：T-M3.2 事务（v2 首项，里程碑级）
- 前置精读：`/home/nzinfo/src.db/docs/12-arroyo.md` §11（对象存储 manifest/fencing/两段提交协议——basalt 路线的参照实现）。
- 设计要点已在 TASK.md T-M3.2 行：txn coordinator + 内部日志；事务版本直接 TV2（KIP-890，ADR-9 不做 TV1）；LSO 推进；control record 占 offset 不投递；read_committed 过滤；KIP-447 验证；接管恢复判定纯函数化（Ready/ReplayCommit/Orphaned 三态）。
- 依赖：T-M3.1 幂等 ✅（本会话收口）。
- 专项测试清单在 T-M3.6：Jepsen Bufstream 三场景（aborted reads / torn transactions / lost writes）。

### P1：挂起项
- **ReplicationCommit 名义活性反例**（WIP，规格注释有完整分析）：Produce×2 → Tick×2 → 永久 stutter——follower 从不 pull 的沉默簇行为合法不 ack。两个方向：①条件化重述（稳定 pull 环境 ⇒ ack 推进）；②fresh/ISR 双阈值分离建模（同时可恢复 norecon 判别力）。恢复活性已证 ✅ 不受影响。
- **重试风暴仿真**（T-M3.1 收口）：确定性注入可复用 BASALT_PULL_REPLAY 模式。
- **openraft 引擎补齐**：ADR-16 已修订默认引擎为 raftrs（验收面全在 raftrs）；openraft 保持骨架（选举/复制/快照内嵌测试绿），补齐 = 控制器 WAL + 九场景门禁，工作量大、按需启动。

### P2：打磨
- 分区 actor 孤儿数据目录的 retention 清理验证；C14 租约字段（L2 健康位图）；ReplicationCommit 扩展 RF=3+partial-face 情形。

## 4. 已知陷阱（本会话实证，勿重踩）

1. **TASK.md 复选框滞后于现实**——以「验收进度」段落 + 账本为准；改状态时同步。
2. **pkill -f 会误杀**（命令行含同样字符串）——用 `pkill -x basalt-server`；harness 会在命令边界清理后台进程树——跨命令检查必须在同一命令内完成（或 runner 加 SKIP 窗口）。
3. **多节点 runner**：`run_multinode.sh` 已注入 BASALT_ISR_LAG_MS=500（调优后活性前提）；SKIP_CLEANUP=1 保留现场（runner 末尾 sleep 30 调试窗口——run_kafkaclients.sh 有样例）。
4. **cargo test 与 e2e 并行会资源竞争**（端口/CPU 假象失败）——分开跑；挂死测试用 `-- --test-threads=1` 定位。
5. **spec/ 的 UNCHANGED 审计教训**：审计脚本曾把 primed 变量错补进 UNCHANGED（Produce 自相矛盾不可执行、HWAdvance 含 hw 自锁致 hw 恒 0、CrashL 不可执行致崩溃路径不可达）——改动作必须核对「UNCHANGE 集 = 全变量 − 本动作 primed 集」。
6. **「全空间绿」可能是平凡绿**：崩溃路径不可达时安全不变式无判别力——任何不变式都要配一个能触发的阴性对照。
7. **kafka-python 3.14 兼容**：`from kafka.admin import KafkaAdminClient, NewTopic`；confluent_kafka 的 poll 返回单条 Message（用 consume(num_messages=)）。
8. **Java 档生命周期**：run_kafkaclients.sh 有就绪等待 + 预建题（kafka-clients 无 auto-create 协商）+ SKIP_CLEANUP 调试窗口。

## 5. 快速验证命令（接手后先跑一遍确认基线）

```bash
cd /home/nzinfo/src.db/stream-db/basalt
cargo test --workspace                      # 20+54 测试全绿
make -C spec replication-commit replication-commit-norecon \
      replication-commit-liveness replication-commit-recovery   # 一绿一红两绿
SKIP_CLEANUP=1 RAFTRS=1 bash testing/e2e/run_multinode.sh multinode_isr.py   # PASS
bash testing/e2e/run_kafkaclients.sh        # PASS
```
